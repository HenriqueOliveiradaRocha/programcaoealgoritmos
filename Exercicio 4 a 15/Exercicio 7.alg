algoritmo "semnome"
// Função :
// Autor :
// Data : 10/09/2018
// Seção de Declarações 
var
Base,Altura,Resp :real
inicio
// Seção de Comandos
escreval("Cálcule a área de um triângulo")
escreval("Digite o valor da Base")
leia(Base)
escreval("Digite a altura")
leia(Altura)
Resp <- (Base*Altura)/2
escreval ("A área do triângulo é ",Resp)
fimalgoritmo
