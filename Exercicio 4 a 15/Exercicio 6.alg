algoritmo "semnome"
// Função :
// Autor :
// Data : 10/09/2018
// Seção de Declarações 
var
Base,Altura,Resp :real
inicio
// Seção de Comandos
escreval("Cálcule a área de um retangulo")
escreval("Digite o valor da base")
leia(Base)
escreval("Digite o valor da altura")
leia(Altura)
Resp <- Base*Altura
escreval("O tamanho da área é de ",Resp)
fimalgoritmo
